server:
  port: 8080

spring:
  application:
    name: semantic-kernel-java-teaching-demo

semantickernel:
  api-key: ${OPENAI_API_KEY:demo-placeholder}
  model: ${SEMANTIC_KERNEL_MODEL:qwen+-placeholder}
  endpoint: ${SEMANTIC_KERNEL_ENDPOINT:http://localhost:11434/v1}
  temperature: 0.7
